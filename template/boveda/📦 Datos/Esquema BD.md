# Esquema de Base de Datos

**Propósito**: Mapa legible de tablas, relaciones y enums.

---

Ejecutar `npm run db:schema` para regenerar este archivo desde `information_schema`.

(Pendiente de documentar)
